namespace Handlebars.conf;

/// <summary>
/// Types of backends available for loading template data.
/// </summary>
internal enum BackendType
{
    /// <summary>
    /// Backend that loads data from environment variables.
    /// </summary>
    Environment
}
