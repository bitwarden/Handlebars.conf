server {
    server_name test.passwordless.example.com;

        listen 5701 ssl http2;
        #listen [::]:5701 ssl http2;

        ssl_certificate /etc/bitwarden_passwordless/ssl.crt;
        ssl_certificate_key /etc/bitwarden_passwordless/ssl.key;
        ssl_session_timeout 30m;
        ssl_session_cache shared:SSL:20m;
        ssl_session_tickets off;
        
            # Diffie-Hellman parameter for DHE ciphersuites, recommended 2048 bits
            ssl_dhparam /etc/bitwarden_passwordless/dh.pem;
    
        ssl_protocols TLSv1.2;
        ssl_ciphers "ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-SHA384:ECDHE-RSA-AES256-SHA384:ECDHE-ECDSA-AES128-SHA256:ECDHE-RSA-AES128-SHA256";
        
        # Enables server-side protection from BEAST attacks
        ssl_prefer_server_ciphers on;
        
            # OCSP Stapling ---
            # Fetch OCSP records from URL in ssl_certificate and cache them
            ssl_stapling on;
            ssl_stapling_verify on;
        
            # Verify chain of trust of OCSP response using Root CA and Intermediate certs
            ssl_trusted_certificate /etc/bitwarden_passwordless/ca.crt;
            resolver 1.1.1.1 1.0.0.1 9.9.9.9 149.112.112.112 valid=300s;
    
        include /etc/nginx/security-headers-ssl.conf;

    include /etc/nginx/security-headers.conf;

            set_real_ip_from 192.168.1.1;
            set_real_ip_from 192.168.1.2;
            set_real_ip_from 192.168.1.3;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;

    location /alive {
        default_type text/plain;
        return 200 $date_gmt;
    }

        location /api/ {
            proxy_pass http://localhost:5000/;
        }

        location / {
            proxy_pass http://localhost:5001/;
        }
}
